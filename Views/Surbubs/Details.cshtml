@model Deliverable2.Models.Surbub

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Details</title>
</head>
<body>
    <div>
        <h4>Surbub</h4>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.SurbubName)
            </dt>
    
            <dd>
                @Html.DisplayFor(model => model.SurbubName)
            </dd>
    
        </dl>
    </div>
    <p>
        @Html.ActionLink("Edit", "Edit", new { id = Model.SurbubID }) |
        @Html.ActionLink("Back to List", "Index")
    </p>
</body>
</html>
