@model IEnumerable<Deliverable2.Models.Surbub>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SurbubName)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SurbubName)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.SurbubID }) |
                @Html.ActionLink("Details", "Details", new { id=item.SurbubID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.SurbubID })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
