@model IEnumerable<Deliverable2.Models.UAddress>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Street.StreetName)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Street.StreetName)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.AID }) |
                @Html.ActionLink("Details", "Details", new { id=item.AID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.AID })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
