@model IEnumerable<Deliverable2.Models.Hospital>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.HospitalName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UAddress.AID)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.HospitalName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UAddress.AID)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.HID }) |
                @Html.ActionLink("Details", "Details", new { id=item.HID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.HID })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
