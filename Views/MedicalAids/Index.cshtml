@model IEnumerable<Deliverable2.Models.MedicalAid>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MedicalAidName)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MedicalAidName)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.MID }) |
                @Html.ActionLink("Details", "Details", new { id=item.MID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.MID })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
