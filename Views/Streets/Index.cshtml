@model IEnumerable<Deliverable2.Models.Street>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StreetName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.HouseNO)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Surbub.SurbubName)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StreetName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HouseNO)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Surbub.SurbubName)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.StreetID }) |
                @Html.ActionLink("Details", "Details", new { id=item.StreetID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.StreetID })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
